<div class="m-header">
    <a href="{{ route('home') }}" class="b-brand text-primary">
        <!-- ========   Change your logo from here   ============ -->
        <img src="{{ app_logo() }}" alt="{{ app_name() }}" class="logo-lg"/>
        {{--                    <span class="badge bg-primary rounded-pill ms-2 theme-version">v3.1.0</span>--}}
    </a>
</div>
